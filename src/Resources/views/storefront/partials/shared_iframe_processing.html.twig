{% block base_footer %}
    {% if isIframeEnabled() %}
        {% set currentRoute = app.request.attributes.get('_route') %}
        <script>
            const paymentMethodId = '{{ paymentMethodId() }}';
            const cartValidationUrl = '{{ path('emerchantpay.frontend.cart.validate') }}';
            const currentRoute = '{{ currentRoute }}';
        </script>
        <script src="{{ asset('bundles/emerchantpaygenesis/redirect-interceptor.js') }}"></script>
    {% endif %}
{% endblock %}
